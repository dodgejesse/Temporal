package edu.uw.cs.lil.tiny.utils.string;

/**
 * Given a string, will modify it and return its modified version. This is a
 * filter in the sense of Photoshop filters.
 * 
 * @author Yoav Artzi
 */
public interface IStringFilter {
	String filter(String str);
}
